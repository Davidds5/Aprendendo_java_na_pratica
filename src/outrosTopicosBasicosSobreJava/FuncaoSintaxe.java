package outrosTopicosBasicosSobreJava;

public class FuncaoSintaxe {
    public static void main(String[] args) {
        double y = 25.0;
        // Math.sqrt (retorna a raiz quadrada)
        // chamada da função
        // reaproveitamento
        // dados de entrada e saida(podem receber dados, retorna ou não uma saida)
        double x =Math.sqrt(y);


        System.out.println("Retorna raiz quadrada: " + x);
    }
}
